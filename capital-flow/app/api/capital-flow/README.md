/**
 * Optional local API shape (not used on GitHub Pages static export).
 *
 * Production uses `scripts/generate-capital-flow.mjs` → public/data/capital-flow.json
 * regenerated by GitHub Actions (~30m) with yahoo-finance2 + CoinGecko (no API keys).
 *
 * If you run a Node server later (not `output: 'export'`), you can wire:
 *   GET /api/capital-flow → buildPayload from the bake script logic
 *   export const revalidate = 60
 */
export {};
